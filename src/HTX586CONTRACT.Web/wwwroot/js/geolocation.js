window.contractLocation={getCurrentPosition(){return new Promise((resolve,reject)=>{if(!navigator.geolocation){reject('Trình duyệt không hỗ trợ GPS.');return;}navigator.geolocation.getCurrentPosition(p=>resolve({latitude:p.coords.latitude,longitude:p.coords.longitude,accuracy:p.coords.accuracy,deviceTimestamp:p.timestamp}),e=>reject(e.message),{enableHighAccuracy:true,timeout:15000,maximumAge:0});});}};
