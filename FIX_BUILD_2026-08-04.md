# Sửa lỗi build ngày 04/08/2026

## Đã sửa

1. `ContractRecords/Index.razor`
   - Sửa `CS0173` do toán tử điều kiện trả về `Guid[]` và `HashSet<Guid>`.
   - Khai báo biến dưới dạng `IEnumerable<Guid>`.

2. `ContractService.cs`
   - Sửa `CS8602` bằng cách kiểm tra đầy đủ `Vehicle`, `VehicleOwner`, `CompanyProfile` trước khi sử dụng.
   - Loại bỏ null-forgiving operator không cần thiết ở đoạn này.

3. `Accounts/Detail.razor` và `Accounts/Edit.razor`
   - Sửa các cảnh báo `CS8604` khi `ServiceResult.Message` có thể null.
   - Bổ sung thông báo mặc định theo kết quả thành công/thất bại.
